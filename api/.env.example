# Backend Environment Configuration for Teammates
# Copy this file to .env in the api/ directory

# CRITICAL: Change this secret key for your environment
DJANGO_SECRET_KEY=your-secret-key-here-change-this-for-security

# Development vs Production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*

# CORS Settings (for frontend communication)
CORS_ALLOW_ALL=True
# If not allowing all, set explicit origins (comma-separated)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# API base path
API_PREFIX=/api

# Database Configuration
# Option 1: Use Neon PostgreSQL (Recommended - same as original)
# Get your connection string from: https://console.neon.tech/
# DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Option 2: Use local SQLite (Simple setup for testing)
DATABASE_URL=sqlite:///db.sqlite3

# Option 3: Use local PostgreSQL
# DATABASE_URL=postgresql://postgres:password@localhost:5432/timetable_db

# Production Settings (when deploying)
# DJANGO_DEBUG=False
# DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
